# Audit Navigation + Profile Scroll Lock Fix

- Audit & Reports now remains visible in the Admin sidebar on Overview, User Directory, Registration Queue, and Audit & Reports.
- Opening the shared profile settings drawer freezes the dashboard at its current scroll position.
- Only the profile drawer is vertically scrollable while open. Closing it restores the exact prior dashboard position.
- This applies to Student, Officer, and Admin pages that use the shared profile manager.
- The Audit page System guardrails sticky-card behavior was intentionally preserved.
